// create a list (array) simply
let point = [1, 2, 3, 4];

// to get array's length
let len = point.length;

// to add an item to array:
point.push(5);

// to remove item from array (this method return that item implicitly)
let lastItem = point.pop();

// call an item from array
let item = point[0];

console.log("Len:", len,"last:",lastItem, item, point);


